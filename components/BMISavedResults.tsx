import { StyleSheet, Text } from "react-native";

import React from "react";

export default function BMISavedResults() {
  return <Text>Saved results</Text>;
}

const styles = StyleSheet.create({});
